<nav class="navbar navbar-expand-lg navbar-light bg-wildflix">
    <a class="navbar-brand color-custom" href="/">WildFlix</a>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link active color-custom" href="/program">Toutes les séries <span class="sr-only">(current)</span></a>
        </div>
        <div class="nav-link colorMail">
            {% if app.user %}

                <a class="loginLogout" href="{{ path('app_logout') }}"> Se déconnecter</a>
                <span class="userEmail">{{ app.user.email }}</span>
            {% else %}
                <a class="loginLogout" href="{{ path('app_login') }}"> Se connecter</a>
            {% endif %}
        </div>
    </div>
</nav>