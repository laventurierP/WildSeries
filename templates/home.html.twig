{% extends 'base.html.twig' %}
{% block body %}
    <h1> Bienvenue !</h1>
    <a href="{{ path('wild_show', { 'slug': "penny-dreadful" }) }}">
        Testing show() method from WildController with a real program s slug.
    </a>
    <br>
    <a href="{{ path('wild_index') }}">
        Testing index() method from WildController to view all programs.
    </a>
    <br>
    <a href="{{ path('wild_category', {'categoryName': 'horreur'}) }}">
        Testing showByCategory() method from WildController to view three programs from category horreur.
    </a>
{% endblock %}